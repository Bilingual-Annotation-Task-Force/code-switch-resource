include app-layout

div#app-main-area
    form(method="post", enctype="multipart/form-data", action="/client/my-corpora/upload")
        p
            input(type="text", name="title", placeholder="title")
        p
            input(type="file", name="corpus_data")
        p
            input(type="submit")